#importando o sum
from django.db.models import Sum
from core.produto.models import Produto

#listando o valor total por categoria de produto
Produto.objects.values('categoria').annotate(Sum('valor'))


orm = Produto.objects.values('categoria').annotate(Sum('valor'))

print(orm.query) #para visualizar a query
