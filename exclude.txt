# exclude()
from core.produto.models import Produto

#exclui da listagem todos os produtos com o id maior e igual a 4
Produto.objects.exclude(id__gte=4)


orm = Produto.objects.exclude(id__gte=4)

print(orm.query) #para visualizar a query
